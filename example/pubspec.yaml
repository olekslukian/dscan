name: dscan_example
description: "Demonstrates how to use the dscan plugin."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.11.3
dependencies:
  cupertino_icons: ^1.0.8
  dscan:
   path: ../
  flutter:
    sdk: flutter
  image_picker: 1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter

flutter:

 uses-material-design: true
